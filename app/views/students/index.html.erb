<h1>All students</h1>

<%= form_tag("/search", method: "get") do  %>
  <%= label_tag(:q, "Search students") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %>

<% @students.each do |student| %>
  <%= render partial: "student", locals: { student: student } %>
<% end %>

